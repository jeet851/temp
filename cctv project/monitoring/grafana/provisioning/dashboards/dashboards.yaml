apiVersion: 1

providers:
  # ── Auto-provision Dashboards from /var/lib/grafana/dashboards ──
  - name: 'VisionGuard Dashboards'
    orgId: 1
    folder: 'VisionGuard'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
